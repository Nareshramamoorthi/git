#include <stdio.h>

int main() {
   int a;
   printf("enter the postive real no :");
   scanf("%d",&a);
   if(a%2==0){
       printf("The give postive real no is  even");
   }
   else{
       printf("The given postive real no is odd");
   }
    return 0;
}